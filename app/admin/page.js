"use client";
import {useEffect,useState} from "react";
import {getAuth,onAuthStateChanged,GoogleAuthProvider,signInWithPopup,signInWithRedirect,signOut} from "firebase/auth";
import {collection,deleteDoc,doc,onSnapshot,orderBy,query,setDoc,serverTimestamp} from "firebase/firestore";
import {app,db} from "../../lib/firebase";
const ADMIN="ngogrant454@gmail.com";
export default function Admin(){const[u,setU]=useState(undefined);const[open,setOpen]=useState(false);const[error,setError]=useState("");const[profiles,setProfiles]=useState([]);const[regs,setRegs]=useState([]);const[form,setForm]=useState({id:"",gender:"female",photo:""});const auth=getAuth(app);useEffect(()=>onAuthStateChanged(auth,setU),[auth]);useEffect(()=>{if(u?.email?.toLowerCase()!==ADMIN)return;const a=onSnapshot(collection(db,"profiles"),s=>setProfiles(s.docs.map(d=>({id:d.id,...d.data()}))));const b=onSnapshot(query(collection(db,"registrations"),orderBy("createdAt","desc")),s=>setRegs(s.docs.map(d=>({id:d.id,...d.data()}))));return()=>{a();b()}},[u]);const login=async()=>{setError("");const provider=new GoogleAuthProvider();provider.setCustomParameters({prompt:"select_account"});try{await signInWithPopup(auth,provider)}catch(e){if(["auth/popup-blocked","auth/popup-closed-by-user","auth/cancelled-popup-request"].includes(e.code)){try{await signInWithRedirect(auth,provider);return}catch(x){setError(x.code||x.message)}return}setError(e.code||e.message)}};const logout=()=>{setOpen(false);signOut(auth)};async function saveProfile(e){e.preventDefault();if(!form.id||!form.photo)return;await setDoc(doc(db,"profiles",form.id),{gender:form.gender,photo:form.photo,updatedAt:serverTimestamp()},{merge:true});setForm({id:"",gender:"female",photo:""})}async function remove(id){if(confirm("Profile delete karein?"))await deleteDoc(doc(db,"profiles",id))}if(u===undefined)return <main><section className="admin cardPage"><div className="adminIcon">⌛</div><h1>Admin</h1><p>Loading...</p></section></main>;if(!u)return <main><section className="admin cardPage"><div className="adminIcon">🔐</div><span className="eyebrow">ZARA SHADI SERVICE</span><h1>Admin Login</h1><p>Admin panel access ke liye Google account se login karein.</p><button className="primaryAction" onClick={login}>Google se Login <span>→</span></button>{error&&<p style={{color:"crimson",marginTop:12}}>Login error: {error}</p>}</section></main>;if(u.email?.toLowerCase()!==ADMIN)return <main><section className="admin cardPage"><div className="adminIcon">🚫</div><h1>Access Denied</h1><p>यह Google account admin नहीं है.</p><button className="backAction" onClick={logout}>🚪 Logout</button></section></main>;return <main><section className="admin cardPage"><div className="adminIcon">📊</div><span className="eyebrow">ZARA SHADI SERVICE</span><h1>Admin Dashboard</h1><p className="logged">Logged in: <b>{u.email}</b></p>{!open?<><button className="primaryAction" onClick={()=>setOpen(true)}>📊 Admin Dashboard <span>→</span></button><button className="logout" onClick={logout}>🚪 Logout</button></>:<><form className="adminBox" onSubmit={saveProfile}><h3>➕ Profile Add / Update</h3><input className="adminInput" placeholder="Profile ID (e.g. ZS-105)" value={form.id} onChange={e=>setForm({...form,id:e.target.value})}/><select className="adminInput" value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})}><option value="female">Female</option><option value="male">Male</option></select><input className="adminInput" placeholder="Photo URL" value={form.photo} onChange={e=>setForm({...form,photo:e.target.value})}/><button className="primaryAction">Save Profile</button></form><div className="dashboardGrid"><div><b>Profiles</b><span>{profiles.length}</span></div><div><b>Registrations</b><span>{regs.length}</span></div></div><div className="adminList"><h3>Profiles</h3>{profiles.map(p=><div className="adminRow" key={p.id}><span>{p.id} • {p.gender}</span><button onClick={()=>remove(p.id)}>Delete</button></div>)}</div><div className="adminList"><h3>Registrations</h3>{regs.map(x=><div className="adminRow" key={x.id}><span>{x.name} • {x.phone} • {x.profileId}</span><small>{x.status}</small></div>)}</div><button className="logout" onClick={logout}>🚪 Logout</button></>} </section></main>}